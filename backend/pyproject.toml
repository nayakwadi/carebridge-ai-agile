[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # FastAPI uses `Depends()` as a default argument by design

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
